import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function MaterialUploadPanel() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Material Upload</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Sistema em configuração</p>
      </CardContent>
    </Card>
  );
}