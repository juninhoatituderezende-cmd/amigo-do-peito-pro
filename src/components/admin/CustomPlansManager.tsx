import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function CustomPlansManager() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Custom Plans Manager</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Sistema em configuração</p>
      </CardContent>
    </Card>
  );
}